#pragma once

#include <map>
#include <string>

namespace commonHead::model{
enum class LocalizedString{
    None,
    OkButtonLabel,
    CancelButtonLabel
};

enum class LocalizedStringWithParam{
    None,
    TestParm
};

}