// DO NOT EDIT - Auto generated
// Generated with {{ self._TemplateReference__context.name }}

#include <UIResourceStringLoader/UIResourceStringLoader.h>

#include <UIStringToken.h>
#include <ResourceString.h>
#include "LoggerDefine.h"

namespace UIResource{
{% for enum_name, entries in data.items() %}
commonHead::model::{{enum_name}} UIResourceStringLoader::convertUI{{enum_name}}ToVM{{enum_name}}(UI{{enum_name}}Token::{{enum_name}} uiString)
{
    switch (uiString)
    {
    {% for key in entries.keys() %}
    {% set upperKey = (key | replace(" ", ""))[0] | upper + (key | replace(" ", ""))[1:] %}
    case UI{{enum_name}}Token::{{enum_name}}::{{ upperKey }}:
        return commonHead::model::{{enum_name}}::{{ upperKey }};
    {% endfor %}
    default:
        UIResourceStringLoader_LOG_WARN("unsupported token: " << static_cast<int>(uiString));
        return commonHead::model::{{enum_name}}::None;
    }
}
{% endfor %}
}
