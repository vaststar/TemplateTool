#include <fakes/ucf/Services/FakeClientInfoService.h>

namespace ucf::service::fakes {

// FakeClientInfoService 目前只需要头文件中的 mock 定义
// 如果将来需要添加非 mock 的实现逻辑，可以在这里添加

} // namespace ucf::service::fakes
